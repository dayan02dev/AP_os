"""Auth router — filled in Phase 3 (Supabase email-OTP).

Intentionally empty APIRouter so main.py can mount it now and Phase 3 can
add endpoints without touching shared files.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/auth", tags=["auth"])

# Phase 3 endpoints go here.
