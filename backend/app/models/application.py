"""Pydantic models for the Applications API — filled in Phase 4.

Should mirror the column set in backend/migrations/001_initial_schema.sql
(section-prefixed field names). Leaving empty so Phase 4 owns this file
without shared-file conflicts.
"""

# Phase 4 models go here.
