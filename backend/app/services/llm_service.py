"""OpenRouter wrapper (Gemini 2.0 Flash by default) — filled in Phase 5.

Will expose `parse_resume(text_or_pdf_bytes) -> ParsedResume` that calls
OpenRouter with a structured-output prompt targeting the column names in
our `applications` table.
"""

# Phase 5 implementation goes here.
