# Runtime dependencies. Pinned to major versions per Phase 2 spec —
# minor/patch bumps within each range are welcome.

fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*
pydantic-settings==2.6.*

# Supabase client (sync + async); used for auth + data access.
supabase==2.9.*

# File uploads (multipart parsing) + outbound HTTP client.
python-multipart==0.0.*
httpx==0.27.*

# Email address validation (backs pydantic.EmailStr). Needed by the auth
# router (OTP email), support router (ticket form), and application submit
# validation.
email-validator==2.*

# Resume parsing (Phase 5).
pypdf==5.*
python-docx==1.1.*
# Fallback when an LLM returns *almost* valid JSON (unescaped quotes
# inside a long string, trailing commas, dropped braces under load).
# Cheaper and more deterministic than retrying the whole API call.
json-repair==0.*

# AWS SES for transactional email (Phase 6).
boto3==1.35.*

# HTML/text email templating (Phase 6). Used by app/services/email_service.py
# to render templates in app/templates/email/.
jinja2==3.1.*

# In-memory rate limiting.
slowapi==0.1.*

# AWS Lambda adapter (used by SAM in Phase 9; no-op locally).
mangum==0.19.*

# Error tracking.
sentry-sdk[fastapi]==2.*

# LangGraph + LangChain for the AI scoring pipeline (spec §2).
# Pinned to major to allow patch updates; revisit when major bumps.
langgraph==0.2.*
langchain==0.3.*
langchain-openai==0.2.*
# Pin tiktoken to a manylinux2014-compatible arm64 wheel.
# 0.12+ only ships manylinux_2_28_aarch64 wheels which require
# glibc >= 2.28; SAM's public.ecr.aws/sam/build-python3.11:latest-arm64
# container is Amazon Linux 2 (glibc 2.26), so those wheels are
# skipped and pip falls back to the sdist — which fails (no Rust
# in the build container). 0.11.0 has manylinux2014_aarch64.whl
# that satisfies the AL2 toolchain. Lift the cap once SAM moves
# to an AL2023 build container or tiktoken adds manylinux2014.
tiktoken<0.12
