# Dev-only dependencies: testing + linting. Not shipped to Lambda.
-r requirements.txt

pytest==8.*
pytest-asyncio==0.24.*
pytest-freezer==0.4.*   # `freezer` fixture for time-sensitive tests (locked_at math, etc.)
httpx==0.27.*     # (also in runtime; redundancy is fine)
ruff==0.7.*
