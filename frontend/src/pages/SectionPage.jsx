// SectionPage — generic route for /apply/:sectionSlug. App reads the slug via
// useParams() internally and advances the phase machine to that section.
import App from "../App.jsx";
export default function SectionPage() { return <App />; }
